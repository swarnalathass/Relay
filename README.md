# Relay
A ChatGPT-style chat app built from scratch — FastAPI backend with streaming responses (SSE) and SQLite-backed conversation history, paired with a lightweight vanilla JS chat UI. Powered by the Claude API.
